#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "mylib.h"
#include "queue.h"

int main (void){

    queue q = queue_new();

    queue_enqueue(2);
    queue_print(q);
    printf("\n");
    queue_enqueue(4);
    queue_print(q);
    printf("\n");
    queue_enqueue(6);
    queue_print(q);
    printf("\n");
    queue_enqueue(8);
    queue_print(q);
    printf("\n");
    queue_enqueue(9);

    queue_print(q);
    printf("\n");

    return EXIS_SUCCESS;
}
